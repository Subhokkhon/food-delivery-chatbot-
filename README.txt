A food delivery chatbot is created using entities and intents in dialogflow, also used MySQL as the database and nodejs in the backend. It basically adds orders,removes orders ,tracks orders.Used html,css at the frontend to design a responsive and beautiful UI.
